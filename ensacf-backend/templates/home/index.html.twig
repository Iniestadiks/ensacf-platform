{# templates/home/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Accueil - ENSACF{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .home-container {
            text-align: center;
            padding: 50px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .home-container h1 {
            font-size: 2.5em;
            margin-bottom: 20px;
        }
        .home-container p {
            font-size: 1.2em;
            margin-bottom: 40px;
            max-width: 800px;
        }
        .home-buttons {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
        }
        .home-buttons a {
            font-size: 1.1em;
            padding: 10px 20px;
            margin: 5px;
        }
        .info-section {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
            margin-top: 50px;
        }
        .info-box {
            background-color: #f8f9fa;
            border: 1px solid #e9ecef;
            border-radius: 10px;
            padding: 20px;
            width: 280px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s;
            text-align: left;
            margin: 10px;
        }
        .info-box:hover {
            transform: translateY(-10px);
        }
        .info-box h2 {
            margin-top: 0;
        }
        .info-box p {
            margin-bottom: 0;
        }
    </style>
{% endblock %}

{% block body %}
<div class="home-container">
    <h1>Bienvenue à l'application de gestion des événements du pole communication de l'ENSACF</h1>
    <p>Ce site est dédié à la gestion et à l'affichage des événements organisés par notre école. Les enseignants peuvent proposer des événements et accéder à leur tableau de bord sous réserve de validation.</p>

    <div class="home-buttons">
        <a href="{{ path('public_events') }}" class="btn btn-primary">Voir les événements</a>
        <a href="{{ path('app_about') }}" class="btn btn-info">À propos</a>
        <a href="{{ path('app_contact') }}" class="btn btn-secondary">Contactez-nous</a>
        {% if app.user %}
            <a href="{{ path('app_event_crud_index') }}" class="btn btn-success">Gestion des événements</a>
        {% else %}
            <a href="{{ path('login') }}" class="btn btn-warning">Connexion</a>
            <a href="{{ path('teacher_register') }}" class="btn btn-primary">Inscription Enseignant</a>
        {% endif %}
    </div>

    <div class="info-section">
        <div class="info-box">
            <h2>Voir les événements</h2>
            <p>Accédez à la liste des événements organisés par notre école et ne manquez aucune activité importante.</p>
        </div>
        <div class="info-box">
            <h2>Proposer un événement</h2>
            <p>Les enseignants peuvent proposer de nouveaux événements. Connectez-vous pour soumettre votre proposition.</p>
        </div>
        <div class="info-box">
            <h2>Gestion des événements</h2>
            <p>Les administrateurs peuvent gérer les événements, approuver les propositions et organiser les activités.</p>
        </div>
        <div class="info-box">
            <h2>Inscription Enseignant</h2>
            <p>Si vous êtes un enseignant, inscrivez-vous pour accéder à votre tableau de bord et proposer des événements.</p>
        </div>
    </div>
</div>
{% endblock %}